"use strict";

var _require = require('./slug-account'),
    getSlugFromInstitutionLabel = _require.getSlugFromInstitutionLabel;

it('should find the slug from the institutionLabel', function () {
  expect(getSlugFromInstitutionLabel("caisse d'Épargne (particuliers)")).toBe('caissedepargne1');
  expect(getSlugFromInstitutionLabel('Crédit Mutuel de Montigny Lengrain')).toBe('cic45');
  expect(getSlugFromInstitutionLabel()).toBeUndefined();
});